# NEXT PROJECT: REPLAY WORKFLOW

次回、同じような洗練されたWebアプリを開発する際に、AI（Antigravity）へ渡すことで作業を自動・効率化するためのガイドラインです。

## 📋 開発の手順（AIはこの指示を読み取ってください）

### ステップ 1: 機能要件の定義
- [ ] 指定されたローカルファイルをデータベースとして使用する。
- [ ] 正規表現を用いて、Markdownのリスト形式（`- [ ] **Task**`）を自在にパース・書き込みできるサーバー機能を実装する。

### ステップ 2: デザイン・システムの構築
- **VisionOS 鉄則の適用**:
  - `backdrop-filter: blur(40px) saturate(180%)` を用いたフロストガラス背景を基本とする。
  - ボーダーは極細（0.5px）とし、白または水色の透過色を使用する。
  - フォントは Sans-serif 系（Inter or Outfit）を使い、ウェイトは 200〜300 を基準にする。

### ステップ 3: UIコンポーネントの統合
- [ ] 入力フォームと実行ボタンは必ず一体化し、背景との調和をとる。
- [ ] ホバーエフェクトには微妙なスケールアップと透過度の変更を採用し、高級感を出す。

### ステップ 4: 最適化と検証
- [ ] 13インチ程度のノートPCでもスクロールなしで収まるよう、垂直方向のマージンとパディングを極限まで削る。
- [ ] CSSメディアクエリ（`@media`）を用いて、スマホ等のモバイル端末でも快適に操作できるようUIを調整する。
- [ ] 不要なアニメーション（Canvasなど）を削ぎ落とし、清潔感を第一にする。

### ステップ 5: デプロイメントと公開
- [ ] `vercel.json` を作成し、サーバー（Node.js）と静的ファイルを適切にルーティングする。
- [ ] Vercel CLI を用いてデプロイし、ウェブ上のURLで最終的な動作確認を行う。

## 💡 AIへの魔法の指示（Magic Prompts）
> 「AppleのVisionOSにあるCalendarアプリのような、清潔感と透け感のあるデザインにして。」
> 「すべての要素を1画面に収め、スマホでも指で押しやすいボタンサイズに調整して。」
> 「背景にはAI生成の高画質な自然風景を使い、UIの色を背景に馴染ませて。」

---
このファイルは `/Users/sawadasaori/.gemini/antigravity/scratch/todo_practice/app/WORKFLOW.md` に保存されています。
次回、新たなプロジェクトを始める際にこの内容をチャットに貼り付けてください。
